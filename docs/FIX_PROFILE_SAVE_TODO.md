# Legacy TODO migrated

Track active work in `docs/TASK_PLAN.md`.

Migration note: Code-side profile save fixes were implemented, but environment rollout/testing actions remain open and are now tracked as a single owner/priority item in `docs/TASK_PLAN.md`.
