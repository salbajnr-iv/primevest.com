# Legacy TODO migrated

Track active work in `docs/TASK_PLAN.md`.

Migration note: Most admin dashboard implementation tasks listed here are complete in code. Remaining validation work (admin rollout checks and production verification) is now consolidated under the P0 admin rollout task in `docs/TASK_PLAN.md` to avoid duplicate checklists.
