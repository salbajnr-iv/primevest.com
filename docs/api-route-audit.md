# API route audit

| Route                                                 | Classification                   | Notes                                                                                                                                          |
| ----------------------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/api/admin/check/route.ts`                        | admin-only                       | Reads the signed-in session and checks `profiles.is_admin`.                                                                                    |
| `app/api/admin/kyc/document/route.ts`                 | admin-only                       | Admin bearer token gate, then uses centralized admin Supabase access to sign document URLs.                                                    |
| `app/api/admin/kyc/requests/route.ts`                 | admin-only                       | Admin bearer token gate, then uses centralized admin Supabase access to read KYC queues.                                                       |
| `app/api/admin/kyc/review/route.ts`                   | admin-only                       | Admin bearer token gate, then dispatches the privileged KYC decision edge function.                                                            |
| `app/api/admin/simulate/route.ts`                     | admin-only                       | Admin bearer token gate, then dispatches privileged maintenance operations.                                                                    |
| `app/api/admin/transactions/withdraw-review/route.ts` | admin-only                       | Admin bearer token gate; executes the review RPC as the admin actor under RLS.                                                                 |
| `app/api/admin/users/delete/route.ts`                 | admin-only                       | Admin bearer token gate, then uses centralized admin Supabase access for audit logging and auth deletion.                                      |
| `app/api/admin/users/impersonate/route.ts`            | admin-only                       | Admin bearer token gate, then uses centralized admin Supabase access for audit logging and target lookup.                                      |
| `app/api/auth/session/route.ts`                       | user-scoped via session/RLS      | Session-aware Supabase route for current session introspection.                                                                                |
| `app/api/auth/signin/route.ts`                        | user-scoped via session/RLS      | Session-aware Supabase route for password sign-in.                                                                                             |
| `app/api/auth/signout/route.ts`                       | user-scoped via session/RLS      | Session-aware Supabase route for cookie-based sign-out.                                                                                        |
| `app/api/auth/signup/route.ts`                        | user-scoped via session/RLS      | Session-aware Supabase route for account creation.                                                                                             |
| `app/api/buy/market-impact/route.ts`                  | server-only privileged operation | No user identity required; uses centralized admin Supabase access only for the market-impact RPC fallback.                                     |
| `app/api/dashboard/activity/route.ts`                 | user-scoped via session/RLS      | Reads orders and transactions for the signed-in user through the session-aware server client.                                                  |
| `app/api/dashboard/aggregates/route.ts`               | user-scoped via session/RLS      | Reads dashboard data for the signed-in user through the session-aware server client.                                                           |
| `app/api/dashboard/asset-center/route.ts`             | user-scoped via session/RLS      | Reads profile and order totals for the signed-in user through the session-aware server client.                                                 |
| `app/api/dashboard/performance/route.ts`              | user-scoped via session/RLS      | Reads wallet and position performance for the signed-in user through the session-aware server client.                                          |
| `app/api/dashboard/summary/route.ts`                  | user-scoped via session/RLS      | Reads the signed-in user's profile and notification counts through the session-aware server client.                                            |
| `app/api/investment-card-images/[slug]/route.ts`      | server-only privileged operation | Public static image responder; no Supabase access.                                                                                             |
| `app/api/kyc/provider-callback/route.ts`              | server-only privileged operation | Provider webhook entrypoint that forwards to a privileged edge function.                                                                       |
| `app/api/kyc/submit/route.ts`                         | server-only privileged operation | Authenticates the signed-in user from the session helper, then performs explicit privileged KYC writes via the centralized admin client.       |
| `app/api/market/latest/route.ts`                      | server-only privileged operation | Public market-data reader; no user identity required.                                                                                          |
| `app/api/orders/history/route.ts`                     | user-scoped via session/RLS      | Reads order history for the signed-in user through the session-aware server client.                                                            |
| `app/api/orders/route.ts`                             | user-scoped via session/RLS      | Reads and creates the signed-in user's orders through the session-aware server client; audit logging uses centralized admin access explicitly. |
| `app/api/positions/route.ts`                          | user-scoped via session/RLS      | Reads positions for the signed-in user through the session-aware server client.                                                                |
| `app/api/primevest-expertise-images/[slug]/route.ts`  | server-only privileged operation | Public static image responder; no Supabase access.                                                                                             |
| `app/api/profile/avatar-url/route.ts`                 | server-only privileged operation | Resolves the signed-in user's avatar path via the session-aware client, then signs the storage URL with centralized admin access.              |
| `app/api/step-card-images/[slug]/route.ts`            | server-only privileged operation | Public static image responder; no Supabase access.                                                                                             |
| `app/api/support/community/route.ts`                  | server-only privileged operation | Public curated content endpoint; no Supabase access.                                                                                           |
| `app/api/support/contact/route.ts`                    | server-only privileged operation | Public contact form endpoint; inserts support tickets through centralized admin access.                                                        |
| `app/api/support/status/route.ts`                     | server-only privileged operation | Public status feed; no Supabase access.                                                                                                        |
| `app/api/support/tickets/[id]/reply/route.ts`         | server-only privileged operation | Authenticates the signed-in user from the session helper, then uses centralized admin access for ticket/reply writes constrained to that user. |
| `app/api/support/tickets/[id]/route.ts`               | server-only privileged operation | Authenticates the signed-in user from the session helper, then uses centralized admin access for ticket/reply reads constrained to that user.  |
| `app/api/support/tickets/route.ts`                    | server-only privileged operation | Authenticates the signed-in user from the session helper, then uses centralized admin access for ticket reads/writes constrained to that user. |
| `app/api/swap/execute/route.ts`                       | user-scoped via session/RLS      | Executes swaps as the signed-in user through the session-aware server client.                                                                  |
| `app/api/swap/quote/route.ts`                         | server-only privileged operation | Public quote builder backed by market snapshots; no user identity required.                                                                    |
| `app/api/transactions/history/route.ts`               | user-scoped via session/RLS      | Reads transaction history for the signed-in user through the session-aware server client.                                                      |
| `app/api/wallets/deposit-intents/route.ts`            | user-scoped via session/RLS      | Creates deposit intents for the signed-in user through the session-aware server client and RPC.                                                |
| `app/api/wallets/withdraw/route.ts`                   | user-scoped via session/RLS      | Creates and looks up withdrawal requests for the signed-in user through the session-aware server client and RLS-protected data.                |
| `app/api/wallets/withdraw-requests/route.ts`          | user-scoped via session/RLS      | Creates withdrawal-review requests for the signed-in user through the session-aware server client and RPC.                                     |
