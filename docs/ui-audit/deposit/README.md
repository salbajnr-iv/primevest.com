# /dashboard/deposit UI audit artifacts

Binary screenshots are not committed in this repository because binary files are not supported in the review pipeline.

## Required captures
- `before.png`
- `after.png`

## Capture command (authenticated session required)
Use Playwright/browser tooling to save route screenshots locally under this folder when running visual QA.
